"""Embedding storage and similarity search: the `VectorStore` adapters.

Sibling of `redstring.graph` in the layered contract, for the same reason:
each holds the adapters for one port and neither may import the other.
"""
