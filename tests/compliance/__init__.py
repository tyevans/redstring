"""Shared port-compliance suites.

Not a test package: nothing here is collected directly (no module matches
`test_*.py` and no class matches `Test*`). Adapters opt in from their own
test module by subclassing the compliance class.
"""
